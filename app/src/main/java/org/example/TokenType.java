package org.example;

enum TokenType {
    NUMBER,
    BOOLEAN,
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    MODULO,
    EOF,
    IDENTIFIER,
    EQUAL,
    LET,
    LEFT_PAREN,
    RIGHT_PAREN,
}
